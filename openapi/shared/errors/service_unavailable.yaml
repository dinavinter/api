description: Service unavailable
content:
  application/json:
    schema:
      allOf:
        - $ref: .\basic-error.yaml
        - type: object
          properties:
            errorCode:
              enum:
                - 503
